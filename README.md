# Frudex

A production-ready Vite + React foundation for the Frudex static website.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

The site is organized around shared components, page-level route views, content data modules, and layered styles so brand content can evolve without reshaping the app.
